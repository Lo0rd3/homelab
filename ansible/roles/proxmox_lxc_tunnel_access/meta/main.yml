---
galaxy_info:
  role_name: proxmox_lxc_tunnel_access
  author: homelab-repo
  description: Apply the root-only Proxmox host config needed to expose tunnel access devices such as /dev/net/tun to a managed LXC.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
    - name: Debian
      versions:
        - bookworm

dependencies: []
